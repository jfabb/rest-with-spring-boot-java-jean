spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_jean?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
    
  jpa:
    
    hibernate:
      ddl-auto: none
      show-sql: false  # Adicionado para mostrar consultas SQL no console
